name: Neptune
version: ${project.version}
main: dev.lrxh.neptune.Neptune
api-version: 26.2

description: Neptune - Practice Core

#libraries:
#  - org.mongodb:mongodb-driver-sync:4.11.1

dependencies:
  server:
    packetevents:
      required: true
      load: BEFORE
      join-classpath: true
    PlaceholderAPI:
      required: false
      load: BEFORE
      join-classpath: true
    Multiverse-Core:
      required: false
      load: BEFORE
      join-classpath: true
    Worlds:
      required: false
      load: BEFORE
      join-classpath: true
    FastAsyncWorldEdit:
      required: false
      load: BEFORE
      join-classpath: true
